export interface IJob {
  _id: string;
  title: string;
  company: string;
  location: string;
}
export interface Job {
  id: string;
  title: string;
  company: string;
  location: string;
}

export interface IJobForm {
  title: string;
  company: string;
  location: string;
}
